version: "3.9"

networks:
  apps:
    external: true

services:
  caddy:
    ports:
      - 80:80
      - 443:443
      - 443:443/udp
    volumes:
      - ${BASE_FAST_DIR}/caddy:/etc/caddy
      - ${BASE_FAST_DIR}/caddy_data:/data
    image: caddy
    hostname: caddy
    networks:
      - apps
    deploy:
      placement:
        constraints:
          - node.hostname == ${CORE_HOSTNAME}
